# Daily memory — 2026-03-18

## Jupiter swap: ~$5 SOL → USDC (prepare)

### Context

- New chat session (`/new`) cleared stale state.
- Cleared old intents via `clear_expired_swap_intents` (0 deleted).
- User asked for a “$5 swap” (~0.0342 SOL → USDC).

### Steps (from tool results)

1. **Prepare:** `jupiter_swap_prepare` with `amount` lamports for ~0.0342 SOL, SOL → USDC mints, `slippage_bps: 50`.
2. **Result (logged):** `ok: true`, `intent_id: a7ac6a99-f61a-4ee4-8c78-51948c2486f5`, quoted `outAmount` / `minOut`, `expires` ~2026-03-18T11:17:25Z.
3. **Balances noted:** SOL 0.175747177, USDC 2.8063 (pre-execute).
4. **Next:** User confirm via intent id or UI execute.

### Lessons

- Fresh session + clear intents helped after prior DB issues.
- Report only on `ok: true`; no invented intent IDs.
- UI card comes from `tool_results` in the assistant response.
- On prepare failure, report the error only.
